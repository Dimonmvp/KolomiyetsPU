package com.mvp.tasks;

import static com.mvp.tasks.QuickSort.*;

public class Start {
    public static void main(String[] args) {
        setArrayLength(10);
        initArray();
        printArray();
        quickSort();
        printArray();
    }
}
